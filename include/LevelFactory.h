#pragma once

class LevelFactory {
public:
    // פונקציה סטטית שמחזירה לוח מוכן לפי מספר השלב
    //static std::unique_ptr<Board> createLevel(int levelNumber) {
    //    auto newBoard = std::make_unique<Board>();

    //    std::string filename = "assets/levels/level_" + std::to_string(levelNumber) + ".txt";

    //    // כאן תבוא הלוגיקה של קריאת הקובץ...
    //    // ושימוש ב-TileFactory כדי למלא את newBoard במשבצות

    //    return newBoard;
    //}
};